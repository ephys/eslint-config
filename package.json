{
  "private": true,
  "scripts": {
    "fix-npm": "lerna exec -- rm -rf node_modules && rm package-lock.json && lerna bootstrap",
    "publish": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "name": "eslint-config"
}
